# 产品嵌入方案 — 三个标准化产品整合到公司网站

> **版本**：v1.0  
> **日期**：2026年6月14日  
> **基于**：PRD v1.0 + 网站设计方案 v1.0  

---

## 一、产品现状分析

### 1.1 三个产品技术架构

| 产品 | 技术栈 | 运行方式 | 默认端口 | 状态 |
|------|--------|---------|---------|------|
| **程晓融** | FastAPI + PaddleOCR + DeepSeek | REST API服务 | 8080 | 90%完成 |
| **乔曦** | Streamlit + DeepSeek | Web应用 | 8501 | 90%完成 |
| **峤远** | Streamlit + DeepSeek | Web应用 | 8501 | 90%完成 |

### 1.2 产品接口分析

#### 程晓融（FastAPI）
```
POST /analyze          # 上传征信报告，返回任务ID
GET /tasks/{id}/status # 查询任务状态
GET /tasks/{id}/download # 下载报告
GET /health           # 健康检查
```

#### 乔曦（Streamlit）
- 无REST API，纯Web界面
- 需要用户交互完成9状态流水线
- 输出为Markdown/Word报告

#### 峤远（Streamlit）
- 无REST API，纯Web界面
- 需要用户上传Excel并选择分析目标
- 输出为HTML/CSV报告

---

## 二、嵌入方案选择

### 2.1 方案对比

| 方案 | 优点 | 缺点 | 适用性 |
|------|------|------|--------|
| **iframe嵌入** | 最简单，无需改动产品代码 | 用户体验一般，移动端适配差 | ⭐⭐⭐ |
| **API代理** | 用户体验好，可自定义UI | 需要产品提供API | ⭐⭐⭐⭐ |
| **微前端** | 完全集成，体验最佳 | 改动最大，复杂度高 | ⭐⭐ |
| **独立子域** | 简单，产品保持独立 | 用户体验割裂 | ⭐⭐ |

### 2.2 推荐方案

**采用混合方案**：

1. **程晓融**：API代理方案（已有REST API）
2. **乔曦/峤远**：iframe嵌入 + 原生UI覆盖层

---

## 三、详细实现方案

### 3.1 程晓融嵌入方案（API代理）

#### 架构设计

```
公司网站 (Next.js)          程晓融 (FastAPI)
      │                           │
      ├── POST /api/cxr/analyze ──► POST /analyze
      ├── GET /api/cxr/status ───► GET /tasks/{id}/status
      └── GET /api/cxr/download ─► GET /tasks/{id}/download
```

#### Next.js API路由

```typescript
// app/api/cxr/analyze/route.ts
import { NextRequest, NextResponse } from 'next/server'

const CXR_BASE_URL = process.env.CXR_BASE_URL || 'http://localhost:8080'

export async function POST(request: NextRequest) {
  const formData = await request.formData()
  
  const response = await fetch(`${CXR_BASE_URL}/analyze`, {
    method: 'POST',
    body: formData,
  })
  
  const data = await response.json()
  return NextResponse.json(data)
}
```

```typescript
// app/api/cxr/status/[id]/route.ts
import { NextRequest, NextResponse } from 'next/server'

const CXR_BASE_URL = process.env.CXR_BASE_URL || 'http://localhost:8080'

export async function GET(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  const response = await fetch(`${CXR_BASE_URL}/tasks/${params.id}/status`)
  const data = await response.json()
  return NextResponse.json(data)
}
```

#### 前端组件

```typescript
// components/features/CXRAnalyzer.tsx
'use client'

import { useState, useCallback } from 'react'
import { Button } from '@/components/ui/Button'
import { Card } from '@/components/ui/Card'

export function CXRAnalyzer() {
  const [file, setFile] = useState<File | null>(null)
  const [taskId, setTaskId] = useState<string | null>(null)
  const [status, setStatus] = useState<'idle' | 'uploading' | 'processing' | 'done' | 'error'>('idle')
  const [progress, setProgress] = useState(0)

  const handleUpload = useCallback(async () => {
    if (!file) return
    
    setStatus('uploading')
    const formData = new FormData()
    formData.append('file', file)
    
    try {
      const response = await fetch('/api/cxr/analyze', {
        method: 'POST',
        body: formData,
      })
      const data = await response.json()
      setTaskId(data.task_id)
      setStatus('processing')
      pollStatus(data.task_id)
    } catch (error) {
      setStatus('error')
    }
  }, [file])

  const pollStatus = async (id: string) => {
    const interval = setInterval(async () => {
      const response = await fetch(`/api/cxr/status/${id}`)
      const data = await response.json()
      
      if (data.status === 'completed') {
        clearInterval(interval)
        setStatus('done')
      } else if (data.status === 'failed') {
        clearInterval(interval)
        setStatus('error')
      } else {
        setProgress(data.progress || 0)
      }
    }, 2000)
  }

  return (
    <Card className="max-w-2xl mx-auto">
      <h2 className="text-2xl font-bold mb-4">AI融资体检</h2>
      <p className="text-gray-600 mb-6">上传征信报告，5分钟获取专业融资建议</p>
      
      <div className="border-2 border-dashed rounded-lg p-8 text-center mb-6">
        <input
          type="file"
          accept=".pdf,.jpg,.png"
          onChange={(e) => setFile(e.target.files?.[0] || null)}
          className="hidden"
          id="file-upload"
        />
        <label htmlFor="file-upload" className="cursor-pointer">
          {file ? (
            <p className="text-green-600">已选择: {file.name}</p>
          ) : (
            <p className="text-gray-500">点击或拖拽上传征信报告</p>
          )}
        </label>
      </div>

      <Button
        onClick={handleUpload}
        disabled={!file || status === 'uploading' || status === 'processing'}
        className="w-full"
      >
        {status === 'uploading' ? '上传中...' : 
         status === 'processing' ? '分析中...' : '开始分析'}
      </Button>

      {status === 'processing' && (
        <div className="mt-4">
          <div className="bg-gray-200 rounded-full h-2">
            <div
              className="bg-blue-600 h-2 rounded-full transition-all duration-300"
              style={{ width: `${progress}%` }}
            />
          </div>
          <p className="text-sm text-gray-500 mt-2">正在分析中，请稍候...</p>
        </div>
      )}

      {status === 'done' && (
        <div className="mt-4 text-center">
          <Button onClick={() => window.open(`/api/cxr/download/${taskId}`)}>
            下载报告
          </Button>
        </div>
      )}
    </Card>
  )
}
```

### 3.2 乔曦/峤远嵌入方案（iframe + 覆盖层）

#### 架构设计

```
公司网站 (Next.js)          乔曦/峤远 (Streamlit)
      │                           │
      ├── iframe ────────────────► Streamlit App
      ├── 覆盖层 (React)          │
      └── 通信 (postMessage) ◄──► │
```

#### iframe包装组件

```typescript
// components/features/StreamlitEmbed.tsx
'use client'

import { useState, useEffect, useRef } from 'react'
import { Card } from '@/components/ui/Card'

interface StreamlitEmbedProps {
  title: string
  description: string
  url: string
  icon: string
}

export function StreamlitEmbed({ title, description, url, icon }: StreamlitEmbedProps) {
  const iframeRef = useRef<HTMLIFrameElement>(null)
  const [isLoading, setIsLoading] = useState(true)
  const [hasError, setHasError] = useState(false)

  useEffect(() => {
    const iframe = iframeRef.current
    if (!iframe) return

    const handleLoad = () => setIsLoading(false)
    const handleError = () => setHasError(true)

    iframe.addEventListener('load', handleLoad)
    iframe.addEventListener('error', handleError)

    return () => {
      iframe.removeEventListener('load', handleLoad)
      iframe.removeEventListener('error', handleError)
    }
  }, [])

  return (
    <Card className="overflow-hidden">
      <div className="bg-gradient-to-r from-blue-600 to-blue-800 text-white p-6">
        <div className="flex items-center gap-3">
          <span className="text-3xl">{icon}</span>
          <div>
            <h2 className="text-2xl font-bold">{title}</h2>
            <p className="text-blue-100">{description}</p>
          </div>
        </div>
      </div>
      
      <div className="relative" style={{ minHeight: '600px' }}>
        {isLoading && (
          <div className="absolute inset-0 flex items-center justify-center bg-gray-50">
            <div className="text-center">
              <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4" />
              <p className="text-gray-500">加载中...</p>
            </div>
          </div>
        )}
        
        {hasError && (
          <div className="absolute inset-0 flex items-center justify-center bg-gray-50">
            <div className="text-center text-red-500">
              <p className="text-lg font-semibold">加载失败</p>
              <p className="text-sm">请稍后重试或联系客服</p>
            </div>
          </div>
        )}

        <iframe
          ref={iframeRef}
          src={url}
          className="w-full border-0"
          style={{ minHeight: '600px' }}
          title={title}
          allow="camera; microphone; geolocation"
        />
      </div>
    </Card>
  )
}
```

#### 产品页面组件

```typescript
// components/features/QiaoxiPage.tsx
import { StreamlitEmbed } from './StreamlitEmbed'

export function QiaoxiPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <StreamlitEmbed
        title="乔曦 · AI商业合同审查"
        description="上传合同，一分钟告诉你——能不能签"
        url={process.env.NEXT_PUBLIC_QIAOXI_URL || 'http://localhost:8501'}
        icon="⚖️"
      />
      
      <div className="mt-8 grid md:grid-cols-3 gap-6">
        <Card>
          <h3 className="font-bold mb-2">签/改/拖/退</h3>
          <p className="text-gray-600 text-sm">国内唯一输出商业决策的合同审查系统</p>
        </Card>
        <Card>
          <h3 className="font-bold mb-2">55,088条法规</h3>
          <p className="text-gray-600 text-sm">纯本地RAG检索，零外部API</p>
        </Card>
        <Card>
          <h3 className="font-bold mb-2">¥20/次起</h3>
          <p className="text-gray-600 text-sm">按次付费，无需年费</p>
        </Card>
      </div>
    </div>
  )
}
```

```typescript
// components/features/QiaoyuanPage.tsx
import { StreamlitEmbed } from './StreamlitEmbed'

export function QiaoyuanPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <StreamlitEmbed
        title="峤远 · AI财务报表分析"
        description="上传报表即出专业财务诊断报告"
        url={process.env.NEXT_PUBLIC_QIAOYUAN_URL || 'http://localhost:8502'}
        icon="📊"
      />
      
      <div className="mt-8 grid md:grid-cols-3 gap-6">
        <Card>
          <h3 className="font-bold mb-2">6种格式自适应</h3>
          <p className="text-gray-600 text-sm">专为中国中小企业非标报表设计</p>
        </Card>
        <Card>
          <h3 className="font-bold mb-2">7大类20+指标</h3>
          <p className="text-gray-600 text-sm">100%自研中国准则适配版</p>
        </Card>
        <Card>
          <h3 className="font-bold mb-2">¥20/次起</h3>
          <p className="text-gray-600 text-sm">按次付费，无需年费</p>
        </Card>
      </div>
    </div>
  )
}
```

---

## 四、部署架构

### 4.1 开发环境

```
localhost:3000    # Next.js 公司网站
localhost:8080    # 程晓融 FastAPI
localhost:8501    # 乔曦 Streamlit
localhost:8502    # 峤远 Streamlit
```

### 4.2 生产环境

```
公司网站 (Vercel)
    │
    ├── /api/cxr/* ──────► 程晓融服务 (云服务器/Docker)
    ├── /products/qiaoxi ──► iframe ──► 乔曦服务 (云服务器/Docker)
    └── /products/qiaoyuan ► iframe ──► 峤远服务 (云服务器/Docker)
```

### 4.3 环境变量配置

```env
# .env.local
NEXT_PUBLIC_CXR_URL=https://cxr.chengxinlin.com
NEXT_PUBLIC_QIAOXI_URL=https://qiaoxi.chengxinlin.com
NEXT_PUBLIC_QIAOYUAN_URL=https://qiaoyuan.chengxinlin.com

# 程晓融配置
CXR_BASE_URL=https://cxr.chengxinlin.com
CXR_API_KEY=your_api_key_here

# DeepSeek API
DEEPSEEK_API_KEY=sk-xxx
```

---

## 五、用户体验优化

### 5.1 加载状态设计

```typescript
// components/features/ProductLoading.tsx
export function ProductLoading({ product }: { product: 'cxr' | 'qiaoxi' | 'qiaoyuan' }) {
  const config = {
    cxr: { name: '程晓融', color: 'blue', steps: ['解析征信报告', '提取关键信息', '匹配融资产品', '生成体检报告'] },
    qiaoxi: { name: '乔曦', color: 'purple', steps: ['解析合同条款', '法规合规审查', '商业模式分析', '生成决策报告'] },
    qiaoyuan: { name: '峤远', color: 'green', steps: ['解析财务数据', '计算财务指标', '分析财务健康', '生成诊断报告'] },
  }

  const { name, color, steps } = config[product]

  return (
    <div className="text-center py-12">
      <div className={`animate-spin rounded-full h-16 w-16 border-4 border-${color}-600 border-t-transparent mx-auto mb-6`} />
      <h3 className="text-xl font-bold mb-2">{name} 正在分析</h3>
      <div className="flex justify-center gap-8 mt-6">
        {steps.map((step, i) => (
          <div key={i} className="text-sm text-gray-500">
            <div className={`w-8 h-8 rounded-full bg-${color}-100 flex items-center justify-center mx-auto mb-2`}>
              <span className={`text-${color}-600 font-bold`}>{i + 1}</span>
            </div>
            {step}
          </div>
        ))}
      </div>
    </div>
  )
}
```

### 5.2 移动端适配

```typescript
// components/features/MobileProductCard.tsx
export function MobileProductCard({ product }: { product: 'cxr' | 'qiaoxi' | 'qiaoyuan' }) {
  const config = {
    cxr: { name: '程晓融', icon: '🏦', desc: 'AI融资体检', price: '¥20/次' },
    qiaoxi: { name: '乔曦', icon: '⚖️', desc: 'AI合同审查', price: '¥20/次' },
    qiaoyuan: { name: '峤远', icon: '📊', desc: 'AI财报分析', price: '¥20/次' },
  }

  const { name, icon, desc, price } = config[product]

  return (
    <div className="bg-white rounded-xl shadow-md p-6 mb-4">
      <div className="flex items-center gap-4 mb-4">
        <span className="text-4xl">{icon}</span>
        <div>
          <h3 className="font-bold text-lg">{name}</h3>
          <p className="text-gray-500 text-sm">{desc}</p>
        </div>
      </div>
      <div className="flex justify-between items-center">
        <span className="text-blue-600 font-bold">{price}</span>
        <Button size="sm">立即体验</Button>
      </div>
    </div>
  )
}
```

---

## 六、安全考虑

### 6.1 跨域配置

```typescript
// 程晓融 FastAPI CORS配置
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",
        "https://chengxinlin.com",
        "https://*.chengxinlin.com"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 6.2 iframe安全

```typescript
// Streamlit配置
# .streamlit/config.toml
[server]
headless = true
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
```

### 6.3 API密钥管理

```typescript
// 使用环境变量，不硬编码
const DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY
```

---

## 七、监控与日志

### 7.1 健康检查

```typescript
// app/api/health/route.ts
import { NextResponse } from 'next/server'

export async function GET() {
  const checks = {
    website: 'ok',
    cxr: await checkService(process.env.CXR_BASE_URL),
    qiaoxi: await checkService(process.env.NEXT_PUBLIC_QIAOXI_URL),
    qiaoyuan: await checkService(process.env.NEXT_PUBLIC_QIAOYUAN_URL),
  }

  return NextResponse.json({
    status: Object.values(checks).every(v => v === 'ok') ? 'ok' : 'degraded',
    services: checks,
    timestamp: new Date().toISOString(),
  })
}

async function checkService(url?: string): Promise<string> {
  if (!url) return 'not_configured'
  try {
    const response = await fetch(url, { method: 'HEAD', signal: AbortSignal.timeout(5000) })
    return response.ok ? 'ok' : 'error'
  } catch {
    return 'unreachable'
  }
}
```

---

## 八、实施计划

### 8.1 第一阶段：基础集成（1周）

- [ ] 配置环境变量
- [ ] 实现程晓融API代理
- [ ] 实现iframe嵌入组件
- [ ] 基础样式适配

### 8.2 第二阶段：体验优化（1周）

- [ ] 加载状态设计
- [ ] 移动端适配
- [ ] 错误处理
- [ ] 性能优化

### 8.3 第三阶段：安全加固（3天）

- [ ] CORS配置
- [ ] API密钥管理
- [ ] 日志监控
- [ ] 健康检查

---

## 九、总结

本方案采用**混合嵌入策略**：

1. **程晓融**：API代理方案，保持REST API优势
2. **乔曦/峤远**：iframe嵌入，最小改动成本

**核心优势**：
- 无需修改现有产品代码
- 保持产品独立性，便于单独迭代
- 用户体验流畅，移动端友好
- 安全性高，密钥不暴露

**后续演进**：
- 根据用户反馈，逐步将乔曦/峤远的UI原生化
- 考虑将核心功能抽取为共享组件
- 统一用户系统和支付系统

---

*本方案基于三个产品的实际代码结构设计，确保可实施性。*
