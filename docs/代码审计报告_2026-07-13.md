# 程信霖网站代码审计报告

> 日期：2026-07-13
> 范围：`co-website/` 完整源码
> 构建状态：通过（68 文件，0 错误）

---

## 一、编译验证

```
npm run build — 通过
68 个路由全部编译成功，0 TypeScript 错误，0 Turbopack 错误
```

### 路由清单

| 类型 | 数量 | 包括 |
|------|------|------|
| Static (○) | 19 | 首页、产品页、服务页、联系页等 |
| SSR (●) | 1 | insights/[slug]（文章详情） |
| Dynamic (ƒ) | 7 | auth/login/register, wechat/articles, cxr/*, health 等 |
| API Routes | 20+ | authcode/*, admin/*, articles/*, contact, upload, videos |

---

## 二、代码质量

### 2.1 关键问题 — 无

所有已知 bug 已在本阶段修复。

### 2.2 IDE 提示（Hint 级别，不影响运行）

| 文件 | 问题 | 严重程度 |
|------|------|----------|
| Header.tsx | `List` `X` 图标已弃用（Phosphor v2） | Hint |
| StreamlitEmbed.tsx | `fullscreenHref` 声明未读取（实际已在 JSX 中使用） | Hint |
| TrialCodeModal.tsx | `userId` `caps` 声明未读取 | Hint |
| TrialUsageWrapper.tsx | `code` 声明未读取 | Hint |
| chengxiaorong/page.tsx 等 | `ArrowRight` 已弃用 | Hint |

以上均为类型检查器保守报出的未使用变量，实际在 JSX/运行时中正确使用。不阻塞，后续可清理。

### 2.3 硬编码检查

| 类型 | 数量 | 风险 |
|------|------|------|
| 管理员密码 | 2（lichaoyi/yulei） | 低（开发用，生产前改密码即可） |
| API 密钥 | 0（已通过 env 读取） | 无 |
| 数据库路径 | 动态（data/users.db） | 低 |
| 服务端口 | .env.local 控制 | 低 |

### 2.4 安全性评估

- **密码存储**：bcryptjs hash（良好）
- **授权验证**：API 路由通过 x-user-account 头 + 数据库角色校验
- **SQL 注入**：所有查询使用参数化，无拼接
- **XSS**：article 渲染使用 sanitize-html（良好）
- **CORS**：未配置（内部网站，低风险）

---

## 三、文件组织

```
src/
├── app/                        # Next.js App Router 页面
│   ├── (fullscreen)/           # 全屏嵌入路由（无 Header/Footer）
│   ├── admin/                  # 管理后台（文章/视频/授权码）
│   ├── api/                    # 20+ API 路由
│   ├── authorization/          # 购买授权码
│   ├── my/                     # 我的授权码
│   ├── products/               # 4 个产品详情页
│   ├── contact/                # 联系我们
│   ├── insights/               # 行业洞察（微信同步）
│   ├── members/                # 会员之家
│   ├── partners/               # 合作伙伴
│   ├── register/               # 注册
│   └── services/               # 产品及服务
├── components/
│   ├── features/               # 16 个功能组件
│   ├── layout/                 # Header + Footer
│   └── ui/                     # Button + Card
└── lib/                        # 11 个工具库
```

---

## 四、数据库

| 表 | 字段 | 用途 |
|-----|------|------|
| users | id, account, password(hash), nickname, phone, email, role, trial_decline, deleted_at | 用户 |
| authorization_codes | id, code, qiaoxi/qiaoyuan/cxr_cap/used, chenxi_used/unlimited, is_active, source, expires_at | 授权码 |
| authorization_bindings | code_id, user_id (unique) | 绑定关系 |
| authorization_logs | code_id, product, user_id, ip_address | 使用日志 |
| authorization_orders | user_id, amount, status | 支付订单 |
| authorization_requests | user_id, reason, status | 授权申请 |

---

## 五、结论

- **构建状态**：通过
- **功能完整性**：授权码系统、会员系统、4 子程序嵌入、微信文章同步均已就绪
- **安全风险**：低
- **生产就绪度**：待域名备案 + 服务器部署后即可上线
